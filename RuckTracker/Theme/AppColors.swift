import SwiftUI

/// Centralized color tokens for the app.
enum AppColors {
    static let background = Color("BackgroundDark")
    static let surface = Color("OffWhite")
    static let surfaceAlt = Color(.systemBackground)
    
    static let primary = Color("PrimaryMain")
    static let textPrimary = Color.white
    static let textSecondary = Color("TextSecondary")
    static let textOnLight = Color("BackgroundDark")
    
    static let accentGreen = Color("AccentGreen")
    static let accentTeal = Color("AccentTeal")
    static let accentWarm = Color("WarmGray")
}

